name: 'find-pr-label-action'
description: 'A GitHub action the finds the occurrence of a specified PR label and returns as an action output'
author: 'MECLab'
inputs:
  match_any:
    required: true
    description: 'finds any match of the specified values in the PR labels.'

runs:
  using: 'node12'
  main: 'dist/index.js'
